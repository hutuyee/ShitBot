# ShitBot TODO（Agent）

## 支持mod （暂不启用）

## 自定义图片模板系统（暂缓）

> 仅记录后续方案，当前版本不实现。目标是让管理员通过可视化编辑器制作图片，保存为版本化模板，并允许 ShitBot 或其他插件按模板 ID 调用。

### 模板与渲染

- [ ] 新建独立的 `image-templates/<模板ID>/` 目录，使用 `manifest.yml`、`scene.yml` 和 `assets/` 组织模板，不影响现有 `templates/*.yml` 在线图/背包主题。
- [ ] 定义带 `schema-version` 的 `scene-v1` YAML 场景格式，支持画布、文字、图片、头像、矩形、圆形、线条、进度条、分组、堆叠、网格、条件和循环节点。
- [ ] 使用 `${context.*}`、`${data.*}` 绑定上下文与数据，PAPI 表达式只出现在明确声明的 PAPI 数据提供器中。
- [ ] 使用 Java2D 在独立渲染线程生成 PNG，先收集平台数据再绘图，不在图片线程访问 Bukkit、代理或 Nukkit API。
- [ ] 支持草稿、发布、版本历史和回滚；生产调用只读取校验通过的不可变发布快照。

### 数据与 PlaceholderAPI

- [ ] 为模板提供统一数据提供器接口，首批支持 ShitBot 上下文、在线玩家、玩家头像和 PlaceholderAPI。
- [ ] Spigot/Paper/Folia 通过平台适配器在正确线程批量解析 PAPI；未安装 PAPI、玩家不存在或变量未解析时返回明确错误。
- [ ] 扩展现有认证代理—后端协议，使 BungeeCord/Velocity 可以把 PAPI 查询发送到指定 Spigot 后端。
- [ ] Nukkit-MOT 只提供 ShitBot 内置变量及以后单独适配的变量源，不冒充支持 Bukkit PlaceholderAPI。
- [ ] 为 PAPI 查询增加数量上限、超时、缓存和逐项错误信息，供编辑器预览页面展示。

### 插件 API

- [ ] 增加轻量的 `ShitBotApi` 模块，公开按模板 ID 异步渲染、查询模板信息和注册自定义数据提供器的稳定接口。
- [ ] 渲染 API 返回 PNG 字节、宽高、内容类型、建议文件名、模板 ID 和模板版本，由调用插件自行决定发送或保存。
- [ ] 在 `commands.yml` 中配置群聊别名、绑定玩家来源、目标服务器、冷却和权限；模板本身不注册命令、不执行任意控制台命令。

### 可视化编辑器

- [ ] 将编辑器静态资源打包进插件，运行时不要求服主安装 Node.js。
- [ ] 提供图层列表、拖拽缩放、对齐吸附、属性面板、数据绑定、条件/循环配置、YAML 源码模式和真实数据预览。
- [ ] 提供模板列表、读取/保存草稿、预览、发布、回滚、资源上传及 PAPI 解析管理接口。
- [ ] 编辑器默认关闭且只监听 `127.0.0.1`，通过 `/shitbot editor` 生成短期一次性登录地址；远程使用交给 HTTPS 反向代理。

### 安全与资源限制

- [ ] 限制画布宽高、总像素、图层数、循环展开数、资源大小、渲染时间、并发数和等待队列。
- [ ] 禁止绝对路径、目录穿越、符号链接越界和任意本机文件读取；远程图片默认禁止并统一走安全资源下载器。
- [ ] 第一版不执行管理员上传的 HTML、JavaScript 或任意服务器命令。
- [ ] 如以后确需 HTML/React 模板，使用独立 `ShitBotRenderer` 浏览器进程，通过本地认证接口与插件通信，避免把 Chromium 和用户代码放进 Minecraft 服务端进程。
